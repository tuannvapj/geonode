# Force HTTP-only configuration for port 8091
# Prevents "plain HTTP request sent to HTTPS port" error

# This file ensures that the HTTP port (80 internally, 8091 externally)
# only accepts plain HTTP requests and does NOT expect HTTPS/SSL

# The GeoNode Nginx image might have SSL configurations that interfere
# This explicitly disables SSL on port 80

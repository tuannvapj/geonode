# Custom Nginx server configuration for port 8091
# This file prevents unwanted redirects and ensures proper proxy behavior

# Disable automatic redirects that strip the port number
absolute_redirect off;
port_in_redirect off;

# Ensure Nginx doesn't redirect to the server name without port
server_name_in_redirect off;
